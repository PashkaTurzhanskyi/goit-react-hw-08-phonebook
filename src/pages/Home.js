import { Typography } from '@mui/material';
import React from 'react';

export const Home = () => (
  <div>
    <Typography variant="h2" component="div" sx={{ ml: 20, mt: 20 }}>
      Welcome to PHONEBOOK!
    </Typography>
  </div>
);
